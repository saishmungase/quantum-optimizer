services:
  - type: web
    name: quantum-vrp
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --workers=2 --threads=4 --bind 0.0.0.0:$PORT
    plan: free
